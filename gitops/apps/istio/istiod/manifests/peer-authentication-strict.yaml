apiVersion: security.istio.io/v1
kind: PeerAuthentication
metadata:
  name: default
  namespace: istio-system
spec:
  # PERMISSIVE allows gradual sidecar rollout; switch to STRICT after platform namespaces are injected.
  mtls:
    mode: PERMISSIVE
