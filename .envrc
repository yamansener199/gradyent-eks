# direnv: allow
# Load with: direnv allow  (https://direnv.net)
export AWS_PROFILE="${AWS_PROFILE:-cirevo}"
export AWS_DEFAULT_REGION="${AWS_DEFAULT_REGION:-eu-central-1}"
export TERRAGRUNT_NON_INTERACTIVE=true
export TF_IN_AUTOMATION=true
